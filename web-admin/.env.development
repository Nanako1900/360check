# 开发默认：MSW 按 openapi.yaml mock，后端就绪后改 false 并配 vite proxy / 真实域名。
# ⚠️ 本文件已纳入版本控制，仅放非敏感默认值。真实密钥（如 VITE_MAP_KEY）请写入
#    .env.development.local（已被 .gitignore 忽略），切勿直接填到本文件再提交。
VITE_API_BASE_URL=/api/v1
VITE_MAP_KEY=
VITE_CDN_BASE=
VITE_ENABLE_MSW=true
