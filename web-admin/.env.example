# 后端 API 基址（同源经网关代理到 /api/v1，或直连后端域名）
VITE_API_BASE_URL=/api/v1
# 腾讯地图 JS API GL key（需在腾讯地图控制台配置域名白名单）
VITE_MAP_KEY=
# 媒体签名 CDN 基址（P5 全景 / 缩略图；通常由 GET /media/{id} 返回完整签名 URL）
VITE_CDN_BASE=
# 是否启用 MSW（按 openapi.yaml 拦截 API，后端未就绪时本地/测试用）
VITE_ENABLE_MSW=false
